{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "report.schema.json",
  "description": "JSON Schema for PD-Spec Report output data",
  "type": "object",
  "required": ["meta", "title", "sections"],
  "properties": {
    "meta": {
      "$ref": "base.schema.json#/$defs/meta",
      "properties": {
        "type": { "const": "report" }
      }
    },
    "title": {
      "type": "string",
      "description": "Report title (in output_language)"
    },
    "sections": {
      "type": "array",
      "items": { "$ref": "base.schema.json#/$defs/section" },
      "description": "Ordered list of report sections. Expected structure: executive summary (text), findings by category (text/table), insight references (table), methodology notes (text)."
    }
  }
}
