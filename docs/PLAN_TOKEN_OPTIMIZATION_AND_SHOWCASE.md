# Plan: Token Optimization + Showcase System

> **Origin:** Session 2026-02-27. Analysis of pds--timining token consumption (densest project: 60+ sources, 47 insights, 199 KB EXTRACTIONS, 68 KB INSIGHTS_GRAPH).
> **BLs:** BL-101 (Work Layer Index System), BL-102 (Showcase Presentation System)
> **Full plan with architecture, formats, schemas, and test plans.**

---

## Context

PD-Spec projects with dense Work layers consume excessive session tokens. Root cause: skills read full data files regardless of how much they actually need. A single `/analyze` run loads ~90,000 tokens of context before doing any real work.

Secondary problem: freemode presentations (HTML monoliths of 250+ KB) require full-file reads for small edits, compounding the token drain.

Both issues are addressed at the engine level, benefiting all current and future projects.

### Token consumption breakdown — `/analyze` on pds--timining

| What gets loaded | Size | Tokens (approx) |
|---|---|---|
| CLAUDE.md | 8 KB | ~2,000 |
| analyze/SKILL.md | 36 KB | ~9,000 |
| EXTRACTIONS.md (full read) | 199 KB | **~50,000** |
| INSIGHTS_GRAPH.md (full read) | 68 KB | **~17,000** |
| CONFLICTS.md | 14 KB | ~3,500 |
| MEMORY.md + other Work files | ~30 KB | ~7,500 |
| **Total** | | **~89,000** |

The skill instructions (~10%) are justified — they ensure deterministic execution. The data reads (~90%) are the optimization target.

---

## BL-101: Work Layer Index System

### Problem

Skills read full accumulation files even when they need a fraction:

| File | Size (pds--timining) | Typical need |
|---|---|---|
| EXTRACTIONS.md | 199 KB (~50K tokens) | 3 of 65 sections (incremental) |
| INSIGHTS_GRAPH.md | 68 KB (~17K tokens) | Scan IDs + titles for dedup |
| _temp/*_normalized.md | 330 KB combined | Reference specific segments |

Synthesis files (STRATEGIC_VISION.md, PROPOSALS.md, RESEARCH_BRIEF.md) are NOT indexable — their value is in the cohesive narrative. They tend to stay under 20 KB and are read in full by design.

### Architecture

**New convention:** `02_Work/_index/` directory for auto-generated indexes.

```
02_Work/
  _index/                              ← auto-generated, safe to delete + regenerate
    EXTRACTIONS_INDEX.md
    INSIGHTS_GRAPH_INDEX.md
    {source}_normalized_INDEX.md
  _temp/                               ← ephemeral session files (unchanged)
    SESSION_CHECKPOINT.md
    {source}_normalized.md
```

**Why `_index/` and not `_temp/`:** The batch processing in `/extract` runs `rm -rf 02_Work/_temp/*` between batches. Indexes need to survive batch boundaries. Separate directory avoids changing existing cleanup logic.

**Threshold:** 20 KB. Files below this are read directly.

**Fundamental rule:** Indexes are an optimization, not a requirement. If the index exists and is fresh → skill reads index. If missing or stale → skill falls back to reading the full file (current behavior). No skill should DEPEND on the index.

### Index Formats

Each file type needs a different structure because the underlying data is different.

#### EXTRACTIONS_INDEX.md

Purpose: Let skills identify which sections exist, when they were extracted, and jump to specific claims by line number.

```markdown
# Extractions Index
> Auto-generated by generate-index.sh. Do not edit manually.
> Source: 02_Work/EXTRACTIONS.md
> Generated: 2026-02-27T18:00
> Source lines: 2114 | Source hash: a1b2c3 (first 8 chars of md5)
> Sections: 65 | Total claims: 1516

| Section | Type | Extracted | Claims | L-start | L-end |
|---|---|---|---|---|---|
| entrevistas operaciones/analisis reu con operaciones.md | meeting_analysis | 2026-02-16T15:00 | 40 | 8 | 55 |
| entrevistas operaciones/reu_coo_roberto-catalan.md | coo_meeting | 2026-02-16T15:00 | 25 | 56 | 87 |
| ... | ... | ... | ... | ... | ... |
```

**Key design decisions:**
- Header includes source file hash for staleness detection
- No per-claim summaries — section-level granularity (65 rows) keeps the index under 5 KB while enabling targeted reads via `L-start`/`L-end`
- Per-claim index would be ~30 KB (1,500 rows) — defeats the purpose

#### INSIGHTS_GRAPH_INDEX.md

Purpose: Let skills scan all insights by ID/title/status/convergence without loading full narratives and evidence trails.

```markdown
# Insights Graph Index
> Auto-generated by generate-index.sh. Do not edit manually.
> Source: 02_Work/INSIGHTS_GRAPH.md
> Generated: 2026-02-27T18:00
> Source lines: 1200 | Source hash: d4e5f6
> Total: 47 | VERIFIED: 39 | PENDING: 0 | MERGED: 1 | INVALIDATED: 7

| ID | Title | Status | Category | Convergence | Voice | L-start |
|---|---|---|---|---|---|---|
| IG-01 | 6 productos en 2 dominios: Ops y Geo | VERIFIED | product-landscape (current) | 4/21 | stakeholder | 15 |
| IG-02 | Aware+Orchestra solución integrada | VERIFIED | product-landscape (current) | 3/21 | stakeholder | 32 |
| IG-SYNTH-01 | Geometry Gap temporal | VERIFIED | user-need (current) | 6/21 | user, stakeholder | 245 |
| ... | ... | ... | ... | ... | ... | ... |
```

**Key design decisions:**
- One row per insight — at 47 insights this is ~5 KB, at 200 insights ~20 KB
- Title is the full insight title (sufficient for semantic dedup by LLM)
- `L-start` enables targeted read of full entry (~15-20 lines each)
- Header stats match `count-statuses.sh` output — cross-validation built in

#### _normalized_INDEX.md

Purpose: Let skills reference specific parts of normalized transcripts without loading 60-130 KB files.

```markdown
# Normalized File Index
> Auto-generated by generate-index.sh. Do not edit manually.
> Source: 02_Work/_temp/reunion_camila_normalized.md
> Generated: 2026-02-27T18:00
> Source lines: 1400 | Source hash: g7h8i9
> Speakers: Camila, Nicolás | Duration: ~45 min (estimated)

| Section/Topic | Speaker(s) | L-start | L-end |
|---|---|---|---|
| Contexto proyecto TIMining | Camila | 1 | 85 |
| Productos actuales y dolor | Camila, Nicolás | 86 | 210 |
| Feedback touchpoint 1 | Camila | 211 | 340 |
| ... | ... | ... | ... |
```

**Indexing strategy:** Index by markdown headers (if normalization added them). Fallback to fixed-size chunks (100 lines) if no headers. Semantic topic segmentation deferred — revisit if header/chunk indexing proves insufficient.

### Script: generate-index.sh

New utility script. Follows the 90/10 rule — 100% mechanical (parsing headers, counting lines, computing hashes).

```
Usage: ./scripts/generate-index.sh <type> <source-file> [output-file]

Types:
  extractions    Parse ## [section] headers, metadata, claim counts
  insights       Parse ### [IG-XX] entries, status, category, convergence
  normalized     Parse ## headers or chunk by 100-line blocks

If output-file is omitted, writes to 02_Work/_index/{basename}_INDEX.md

Exit codes:
  0  Index generated successfully
  1  Source file not found
  2  Source file below threshold (< 20KB) — no index needed
```

**Implementation:** bash + awk/grep. No LLM, no Node.js.

### Staleness Detection

**Mechanism:** MD5 hash (first 8 chars) stored in index header.

Before using an index, the skill runs:
```bash
current_hash=$(md5 -q 02_Work/EXTRACTIONS.md | cut -c1-8)
```
If `current_hash != index_hash` → stale → regenerate or fallback.

**Cost:** One shell command (~50ms). Zero tokens.

**Mid-execution staleness:** If a skill reads the index at start, then writes to the source file, the index becomes stale. This is expected — the skill already has the data in context. It regenerates the index at the end.

### Skill Modifications

#### /extract → generates EXTRACTIONS index + normalized indexes
After Phase 2 completion: call `generate-index.sh extractions` and `generate-index.sh normalized` for each file > 20 KB.

#### /analyze → consumes EXTRACTIONS index + INSIGHTS_GRAPH index, generates INSIGHTS_GRAPH index
- Phase 0: Read indexes instead of full files (with hash validation + fallback)
- Phase 1 (incremental): Filter sections via index table (timestamps already there)
- Phase 2 (dedup): Compare claims against index titles. Only `Read(INSIGHTS_GRAPH.md, offset=L-start, limit=20)` for potential matches.
- After Phase 4: Regenerate insights index.

#### /spec → consumes INSIGHTS_GRAPH index, regenerates after modifications
#### /audit, /ship, /visualize → consume indexes (read-only, no regeneration needed)

### Estimated Token Savings

| Scenario | Current | With indexes | Savings |
|---|---|---|---|
| `/analyze` incremental (3/65 sections) | ~89K tokens | ~15K tokens | **~83%** |
| `/analyze --full` | ~89K tokens | ~89K tokens | 0% (reads everything) |
| `/audit` (read-only scan) | ~75K tokens | ~12K tokens | **~84%** |
| `/ship prd` | ~75K tokens | ~15K tokens | **~80%** |
| Freemode referencing EXTRACTIONS | ~50K tokens | ~5K tokens | **~90%** |

### Gaps & Decisions for Implementation

1. **CONFLICTS.md** (14 KB, below threshold) — apply same pattern if/when it crosses 20 KB. No preemptive work.
2. **SOURCE_MAP.md** (8.4 KB) — unlikely to cross threshold. No action.
3. **Live Research App (`/view`)** — has its own parsers, doesn't use Claude Code Read tool. No changes needed.
4. **Files oscillating around threshold** — `generate-index.sh` returns exit code 2 if < 20 KB. Skill handles gracefully.

---

## BL-102: Showcase — Presentation System

### Problem

Current freemode presentations are HTML monoliths (index.html = 253 KB, styles.css = 68 KB). Content, layout, and styles are tangled. Editing one slide requires reading 250+ KB (~63,000 tokens).

### Architecture

**Separation of concerns:**

```
Content (MDX slides)     →  what to say       (~2 KB per slide)
Engine  (Astro app)      →  how to present it  (components, layouts)
Theme   (CSS tokens)     →  how it looks       (design tokens)
```

**Repository structure:**

```
pd-showcase/                        ← SEPARATE REPO (git submodule of pd-spec)
  src/
    components/
      primitives/                   ← composable: Grid, Column, Card, Image, Quote, Stat, Tag, InsightRef, Icon
      patterns/                     ← convenience: SlideCover, SlideSection, SlideGrid, SlideBenchEvidencia, SlideCompare
    layouts/
      SlideLayout.astro             ← single slide wrapper
      PresentationLayout.astro      ← deck wrapper (navigation, counter, transitions)
    themes/
      theme_spec.css                ← default neutral theme
    pages/
      [deck]/
        index.astro
        [...slide].astro            ← dynamic slide routing
    content/
      config.ts                     ← Zod schemas
  astro.config.mjs                  ← reads content from external dir
  package.json
```

**In each pd-spec project:**

```
showcase/                           ← git submodule → pd-showcase
03_Outputs/_showcase/               ← project-specific content
  decks/
    main/                           ← primary deck
      cover.mdx
      contexto.mdx
      pilares.mdx
      ...
    interna/                        ← second deck
      ...
  theme/
    tokens.json                     ← project theme overrides
    overrides.css
  public/
    img/                            ← images and assets
  deck.config.ts                    ← deck metadata (brand, language, decks)
```

**Content outside submodule:** Astro 5 content layer with `glob()` loader reads from `../../03_Outputs/_showcase/decks`. Submodule = pure engine. Content = project-specific.

### Content Schema

#### Slide Frontmatter (Zod)

```typescript
const slideSchema = z.object({
  order: z.number(),                          // position within deck
  title: z.string(),
  subtitle: z.string().optional(),
  layout: z.enum([
    'cover', 'section', 'content', 'grid',
    'compare', 'bench', 'image-full', 'quote', 'custom'
  ]).default('content'),
  notes: z.string().optional(),               // speaker notes (presenter view)
  tags: z.array(z.string()).optional(),        // ["new", "updated"]
  refs: z.array(z.string()).optional(),        // [IG-XX] traceability
  hidden: z.boolean().default(false),          // skip in presentation, keep in repo
});
```

**Design decisions:**
- `order` in frontmatter, not filename — reordering = change a number
- `layout` is a hint, not a constraint — MDX body always wins
- `refs` enables slide-level traceability to insights

#### Example Slide

```mdx
---
order: 2
title: "Lo Que Escuchamos"
layout: "grid"
tags: ["new"]
refs: ["IG-26", "IG-27", "IG-28", "IG-29"]
notes: "Las 4 directrices vienen del touchpoint 1. Enfatizar que esto es lo que ELLOS dijeron."
---

import { Grid, Card, Icon } from '@showcase/primitives'

<Grid cols={4}>
  <Card>
    <Icon name="clock" />
    ### Tiempo, no paz mental
    Carlos: "Es un mundo sin tiempo para avanzar."
    <InsightRef id="IG-26" />
  </Card>
  <!-- ... more cards -->
</Grid>
```

**Token cost:** ~700 tokens (read + edit) vs ~63,000 tokens for equivalent HTML edit.

### Component System

**Two layers:**

**Primitives** (composable, no layout opinions):
`<Grid>`, `<Column>`, `<Card>`, `<Image>`, `<Quote>`, `<Stat>`, `<Tag>`, `<InsightRef>`, `<Icon>`

**Patterns** (convenience, compose primitives):
`<SlideCover>`, `<SlideSection>`, `<SlideGrid>`, `<SlideBenchEvidencia>`, `<SlideBenchPatrones>`, `<SlideCompare>`

**Principle:** Patterns never limit — they accelerate. User can always drop to primitives for custom layouts.

### Theme System

CSS custom properties. No Tailwind, no build-time resolution.

```css
/* theme_spec.css (default) */
:root {
  --slide-bg: #0a0f12;
  --card-bg: #0f1419;
  --text-primary: #e0e0e0;
  --text-accent: #00f3ff;
  --font-sans: 'Inter', sans-serif;
  --font-mono: 'JetBrains Mono', monospace;
  --color-domain-1: #ff4d4d;
  --color-domain-2: #ff9800;
  --color-domain-3: #00f3ff;
  --color-domain-4: #00ff88;
  /* ... full token set */
}
```

**Inheritance:** `theme_spec.css` (always loaded) → `theme/overrides.css` (project) → deck-level (rare).

**Agent-generated themes:** `/showcase --theme` reads STRATEGIC_VISION + BENCHMARK_UX + PROJECT.md to derive domain colors, brand hints. Rules: never invent brand colors (ask user), domain colors map to pillar names, typography stays system-neutral, dark mode default.

### Skill: /showcase

```
/showcase --init                    Initialize (add submodule, create content structure)
/showcase --generate [deck]         Auto-generate slides from Work layer
/showcase --theme [name]            Generate/regenerate project theme
/showcase [deck]                    Edit mode (read specific slides, accept edit instructions)
/showcase --dev                     Start Astro dev server
```

**Relationship to /ship:** `/ship presentation` → PRESENTATION.md (markdown). `/showcase --generate` → MDX slides (visual, component-based). They coexist. PRESENTATION.md can serve as input for `/showcase --generate`.

### Gaps for Implementation

1. **Slide transitions** — minimal for MVP (fade or none)
2. **InsightRef rendering** — tooltip with insight title requires build-time read of INSIGHTS_GRAPH_INDEX.md
3. **Presenter mode** — speaker notes route, Phase 2
4. **Responsive** — fixed 16:9 aspect ratio, scale to fit viewport
5. **Migration** — start fresh with `--generate`, existing HTML as visual reference only
6. **Mermaid** — Astro remark plugin, Phase 2
7. **Export** — Print CSS for PDF. PPTX via adapted html2pptx.py. Phase 5.
8. **Images** — Astro auto-optimization (WebP, srcset, lazy). Git LFS flag if repo > 500 MB.

---

## Execution Roadmap

### Phase 1: Index Foundation
- `generate-index.sh` (extractions type)
- EXTRACTIONS_INDEX.md validated against pds--timining
- `/extract` modified to call generate-index.sh
- `02_Work/_index/` convention documented in CLAUDE_REFERENCE.md

### Phase 2: Index Completion
- `generate-index.sh` (insights + normalized types)
- `/analyze` using indexes with fallback
- `/spec`, `/audit`, `/ship`, `/visualize` updated
- Staleness detection (hash comparison) working
- End-to-end: `/extract` → `/analyze` → `/audit` using indexes

### Phase 3: Showcase Foundation
- pd-showcase repo (Astro 5)
- Primitive components (9) + SlideLayout + PresentationLayout
- theme_spec.css
- Content schema (Zod) + deck config
- `/showcase --init` working
- External content directory validated

### Phase 4: Showcase Generation + Themes
- `/showcase --generate` from Work layer data
- Pattern components (6)
- Theme generation from project context
- InsightRef with index integration
- Migration of 1 pds--timining deck

### Phase 5: Polish + Export
- Print CSS for PDF
- Presenter mode
- Mermaid embed
- Documentation in CLAUDE_REFERENCE.md

---

## Test Plans

### BL-101 Test Plan — Work Layer Index System (CRITICAL)

The index system modifies the reading pattern of 6 skills. Regression testing is essential — the pipeline must produce identical results with and without indexes.

#### T-101-01: generate-index.sh — Extractions

| Step | Action | Expected |
|---|---|---|
| 1 | Run `generate-index.sh extractions` on pds--timining EXTRACTIONS.md | Index generated at `02_Work/_index/EXTRACTIONS_INDEX.md` |
| 2 | Count sections in index vs `grep -c '^## \[' EXTRACTIONS.md` | Counts match |
| 3 | Count total claims in index vs `grep -c '^\d\+\.' EXTRACTIONS.md` | Counts match |
| 4 | Verify L-start/L-end: `sed -n '{L-start},{L-end}p' EXTRACTIONS.md` for 5 random sections | Each range starts with `## [section]` and ends before next `## [` |
| 5 | Run on file < 20 KB | Exit code 2, no index generated |
| 6 | Run on nonexistent file | Exit code 1 |

#### T-101-02: generate-index.sh — Insights

| Step | Action | Expected |
|---|---|---|
| 1 | Run `generate-index.sh insights` on pds--timining INSIGHTS_GRAPH.md | Index generated |
| 2 | Count insights in index vs `grep -c '### \[IG-' INSIGHTS_GRAPH.md` | Match |
| 3 | Verify status counts in header vs `count-statuses.sh` | Match |
| 4 | Verify L-start: `sed -n '{L-start}p' INSIGHTS_GRAPH.md` for 5 random entries | Line starts with `### [IG-XX]` matching index row |
| 5 | Check all statuses parsed correctly (VERIFIED, PENDING, MERGED, INVALIDATED, FROZEN, SUPERSEDED) | All present in index |

#### T-101-03: generate-index.sh — Normalized

| Step | Action | Expected |
|---|---|---|
| 1 | Run on `touchpoint_normalized.md` (136 KB, has headers) | Index with topic sections + line ranges |
| 2 | Run on normalized file without headers | Fallback to 100-line chunks |
| 3 | Verify L-start/L-end ranges are contiguous (no gaps, no overlaps) | Clean coverage |

#### T-101-04: Staleness Detection

| Step | Action | Expected |
|---|---|---|
| 1 | Generate index, verify hash in header | Hash matches `md5 -q file \| cut -c1-8` |
| 2 | Append one line to source file, check hash | Hash no longer matches → stale |
| 3 | Regenerate index | New hash matches modified file |

#### T-101-05: /analyze with Indexes — Behavioral Equivalence (CRITICAL)

**Method:** Run `/analyze` on the same dataset twice — once with indexes, once without. Compare outputs.

| Step | Action | Expected |
|---|---|---|
| 1 | Baseline: Remove `_index/` dir. Run `/analyze` on pds--timining. Save INSIGHTS_GRAPH.md and CONFLICTS.md as `baseline_*` | Baseline captured |
| 2 | Generate indexes. Reset Work layer to pre-analyze state. Run `/analyze` again. | Second run completes |
| 3 | Diff `baseline_INSIGHTS_GRAPH.md` vs new INSIGHTS_GRAPH.md | Identical insight IDs, statuses, categories. Minor wording differences acceptable (LLM non-determinism). |
| 4 | Diff `baseline_CONFLICTS.md` vs new CONFLICTS.md | Same conflicts detected |
| 5 | Verify `/analyze` log shows "Using index" messages | Confirms index path was taken |
| 6 | Delete `_index/` dir mid-session. Run `/analyze` again. | Fallback to full read. No errors. Same results. |

#### T-101-06: /audit with Indexes

| Step | Action | Expected |
|---|---|---|
| 1 | Run `/audit` without indexes. Save scorecard. | Baseline |
| 2 | Generate indexes. Run `/audit` again. | Same scorecard (pass/fail, scores, remediation actions) |

#### T-101-07: /ship with Indexes

| Step | Action | Expected |
|---|---|---|
| 1 | Run `/ship prd` without indexes. Save PRD.md. | Baseline |
| 2 | Generate indexes. Run `/ship prd` again. | Same structure, same [IG-XX] references. Minor wording acceptable. |

#### T-101-08: End-to-End Pipeline

| Step | Action | Expected |
|---|---|---|
| 1 | `/extract` → verify EXTRACTIONS_INDEX.md generated | Index exists, sections match |
| 2 | `/analyze` → verify uses EXTRACTIONS index, generates INSIGHTS_GRAPH index | Both indexes fresh |
| 3 | `/audit` → verify uses both indexes | Scorecard generated without full-file reads |
| 4 | `/ship prd` → verify uses indexes | PRD generated with [IG-XX] refs |

#### T-101-09: Token Measurement

| Step | Action | Expected |
|---|---|---|
| 1 | Run `/analyze` without indexes on pds--timining. Note session token count. | Baseline token count |
| 2 | Run `/analyze` with indexes on same data. Note session token count. | Measurably lower (target: >50% reduction) |
| 3 | Compare total session consumption over 5 skill invocations (with vs without) | Aggregated savings visible |

**Note:** Token measurement depends on Claude Code session reporting. If not available, proxy via file read sizes (sum of bytes read via Read tool).

### BL-102 Test Plan — Showcase System

#### T-102-01: Submodule Init

| Step | Action | Expected |
|---|---|---|
| 1 | Run `/showcase --init` in a fresh pd-spec project | Submodule added at `showcase/`, `03_Outputs/_showcase/` created with structure |
| 2 | Verify `npm install` ran in `showcase/` | `node_modules/` exists |
| 3 | Verify `deck.config.ts` created from PROJECT.md | brand, language, project name match |
| 4 | Verify cover slide created in `decks/main/cover.mdx` | Valid frontmatter, renders |

#### T-102-02: Astro Build + Dev

| Step | Action | Expected |
|---|---|---|
| 1 | `cd showcase && npm run dev` | Dev server starts on port 4321 |
| 2 | Create 3 manual slides in `decks/main/` | Hot reload shows changes |
| 3 | `npm run build` | Static HTML generated in `dist/` |
| 4 | Open `dist/main/index.html` in browser | Presentation renders correctly |

#### T-102-03: Component Rendering

| Step | Action | Expected |
|---|---|---|
| 1 | Create slide using each primitive (`<Grid>`, `<Card>`, `<Image>`, `<Quote>`, `<Stat>`, `<Tag>`) | All render correctly |
| 2 | Create slide using `<Grid cols={3}>` with mixed children (`<Card>` + `<Image>` + `<Quote>`) | Modular composition works |
| 3 | Create slide using a pattern (`<SlideCover>`) | Pattern renders as expected |
| 4 | Override a pattern's internals with custom primitives | Custom composition takes priority |

#### T-102-04: Theme System

| Step | Action | Expected |
|---|---|---|
| 1 | Build with theme_spec only | Default dark theme renders |
| 2 | Add `theme/overrides.css` with `--brand-primary: red` | Brand color changes throughout |
| 3 | Verify inheritance: unoverridden tokens use theme_spec values | Only overridden tokens change |

#### T-102-05: Multi-Deck

| Step | Action | Expected |
|---|---|---|
| 1 | Create two decks (`main/`, `interna/`) with different slides | Both render at `/main` and `/interna` |
| 2 | Verify ordering (`order` frontmatter) is per-deck | Deck A's order doesn't affect Deck B |
| 3 | Verify `deck.config.ts` metadata shows in each deck | Brand, title, subtitle correct per deck |

#### T-102-06: Slide Editing Token Cost

| Step | Action | Expected |
|---|---|---|
| 1 | Edit a single slide's title in `.mdx` | Read + Edit = ~700 tokens |
| 2 | Compare to editing equivalent in current HTML monolith | Read + Edit = ~63,000+ tokens |
| 3 | Reorder 2 slides (change `order` values) | 2 small edits, no other files affected |

#### T-102-07: /showcase --generate

| Step | Action | Expected |
|---|---|---|
| 1 | Run on project with completed Work layer | Slide outline proposed for approval |
| 2 | Approve outline | .mdx files generated, one per slide |
| 3 | Verify `refs:` in frontmatter trace to real [IG-XX] | All refs exist in INSIGHTS_GRAPH |
| 4 | `npm run dev` → presentation renders | Visual output matches proposed outline |

---

## Risk Assessment

| Risk | Impact | Mitigation |
|---|---|---|
| Index line numbers wrong after manual edit | Stale data read | Hash staleness detection + fallback to full read |
| Modifying 6 skill files breaks pipeline | Reliability loss | Phased rollout, behavioral equivalence test (T-101-05) |
| Showcase submodule adds git complexity | Developer friction | `/showcase --init` handles setup, documented in CLAUDE_REFERENCE |
| Theme scope creep | Delays MVP | theme_spec only in Phase 3. Custom themes in Phase 4 |
| Component library maintenance | Long-term cost | 9 primitives only. Patterns are optional sugar |
| Astro upgrades break schema | Migration pain | Pin version. Content schema is minimal Zod |
